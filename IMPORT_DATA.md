# 📥 Як імпортувати notes.json в MongoDB

## Варіант 1: Через MongoDB Compass (Рекомендовано)

### Крок 1: Завантажити MongoDB Compass
1. Перейти на https://www.mongodb.com/try/download/compass
2. Завантажити версію для вашої ОС (Mac/Windows/Linux)
3. Встановити та відкрити Compass

### Крок 2: Підключитися до бази даних
1. У MongoDB Compass в поле "Connection String" вставити ваш connection string з `.env` файлу
   ```
   mongodb+srv://username:password@cluster.mongodb.net/notes-db?retryWrites=true&w=majority
   ```
2. Натиснути **"Connect"**

### Крок 3: Створити базу та колекцію (якщо не існує)
1. У лівій панелі побачите список баз даних
2. Якщо бази `notes-db` немає:
   - Натиснути **"CREATE DATABASE"**
   - Database Name: `notes-db`
   - Collection Name: `notes`
   - Натиснути **"Create Database"**

### Крок 4: Імпортувати дані
1. Відкрити базу `notes-db`
2. Відкрити колекцію `notes`
3. Натиснути кнопку **"Add Data"** (зверху)
4. Вибрати **"Import File"**
5. У вікні вибрати:
   - **File**: знайти та вибрати файл `notes.json` з вашого проєкту
   - **Input File Type**: вибрати **"JSON"**
   - **Import Mode**: вибрати **"Insert documents"**
6. Натиснути **"Import"**
7. Побачите повідомлення про успішний імпорт

### Крок 5: Перевірити дані
Після імпорту ви побачите всі 10 нотаток у колекції `notes`.

---

## Варіант 2: Через MongoDB Atlas UI (В браузері)

### Крок 1: Відкрити MongoDB Atlas
1. Зайти на https://cloud.mongodb.com
2. Увійти в свій акаунт
3. Вибрати ваш проєкт та кластер

### Крок 2: Відкрити Database
1. У лівому меню натиснути **"Database"**
2. Натиснути **"Browse Collections"**

### Крок 3: Створити базу та колекцію
1. Якщо бази `notes-db` немає:
   - Натиснути **"Create Database"**
   - Database Name: `notes-db`
   - Collection Name: `notes`
   - Натиснути **"Create"**

### Крок 4: Додати документи вручну
⚠️ **Примітка**: Atlas UI не підтримує прямий імпорт JSON файлів через інтерфейс. Потрібно додавати документи вручну або використати Compass.

**Спосіб 1: Вручну через UI**
1. Відкрити колекцію `notes`
2. Натиснути **"Insert Document"**
3. Вибрати **"{} JSON"** режим
4. Скопіювати один об'єкт з `notes.json` (без коми в кінці)
5. Натиснути **"Insert"**
6. Повторити для кожного об'єкта

**Спосіб 2: Через API (після запуску сервера)**
Після того, як запустите сервер, можна створити нотатки через POST запити (див. нижче).

---

## Варіант 3: Через API (Після запуску сервера)

Якщо не хочете використовувати Compass або Atlas UI, можна створити нотатки через API:

### Крок 1: Запустити сервер
```bash
npm run dev
```

### Крок 2: Створити нотатки через POST запити

Можна використати Postman, curl або створити простий скрипт.

**Через curl (в терміналі):**
```bash
# Нотатка 1
curl -X POST http://localhost:3000/notes \
  -H "Content-Type: application/json" \
  -d '{"title":"Meeting with team","content":"Discuss project progress and next steps","tag":"Meeting"}'

# Нотатка 2
curl -X POST http://localhost:3000/notes \
  -H "Content-Type: application/json" \
  -d '{"title":"Buy groceries","content":"Milk, bread, eggs, vegetables","tag":"Shopping"}'

# І так далі для кожної нотатки...
```

**Або створити скрипт для масового імпорту** (див. нижче)

---

## Варіант 4: Автоматичний імпорт через скрипт (Найпростіший! ⭐)

### Крок 1: Переконатися, що `.env` файл налаштований
Файл `.env` має містити правильний `MONGO_URL`.

### Крок 2: Запустити скрипт імпорту
```bash
npm run import
```

Скрипт автоматично:
- ✅ Підключиться до MongoDB
- ✅ Прочитає `notes.json`
- ✅ Додасть всі нотатки в колекцію `notes`
- ✅ Покаже результат

**Приклад виводу:**
```
✅ MongoDB connection established successfully
✅ Successfully imported 10 notes!
📝 Notes: Meeting with team, Buy groceries, Finish project documentation, ...
```

### Переваги цього способу:
- ✅ Найшвидший та найпростіший
- ✅ Не потрібно встановлювати додаткові програми
- ✅ Автоматично створює базу та колекцію, якщо їх немає
- ✅ Працює з будь-якою ОС

---

## ⚠️ Важливо!

1. **Назва колекції**: Має бути точно `notes` (множина), бо в моделі використовується `mongoose.model('Note', noteSchema)`, що автоматично створює колекцію `notes`.

2. **Назва бази**: Можна використати будь-яку назву, але в connection string вона вказується після останнього `/`:
   ```
   mongodb+srv://.../notes-db?retryWrites=true&w=majority
   ```

3. **Перевірка**: Після імпорту перевірте, що документи мають поля:
   - `title` (обов'язкове)
   - `content` (може бути порожнім)
   - `tag` (одне з дозволених значень)
   - `createdAt` та `updatedAt` (додадуться автоматично через timestamps)

---

## 🎯 Рекомендація

**Найпростіший спосіб** - використати **MongoDB Compass**, тому що:
- ✅ Простий інтерфейс
- ✅ Прямий імпорт JSON файлів
- ✅ Візуальний перегляд даних
- ✅ Легко редагувати та видаляти документи

