# ✅ Наступні кроки після імпорту даних

## 🎉 Що вже зроблено:
- ✅ Код написано та налаштовано
- ✅ Дані імпортовані в MongoDB
- ✅ Сервер працює локально
- ✅ API повертає дані

---

## 📋 Що залишилося зробити:

### 1️⃣ Протестувати всі роути локально

Перевірте, що всі CRUD операції працюють:

#### GET /notes - Отримати всі нотатки
```bash
curl http://localhost:3000/notes
```
✅ Має повернути масив з 10 нотатками

#### GET /notes/:noteId - Отримати одну нотатку
```bash
# Замініть :noteId на реальний ID з попереднього запиту
curl http://localhost:3000/notes/69307f90ef05f20679e64232
```
✅ Має повернути одну нотатку

#### POST /notes - Створити нову нотатку
```bash
curl -X POST http://localhost:3000/notes \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Note","content":"This is a test","tag":"Work"}'
```
✅ Має повернути створену нотатку зі статусом 201

#### PATCH /notes/:noteId - Оновити нотатку
```bash
curl -X PATCH http://localhost:3000/notes/69307f90ef05f20679e64232 \
  -H "Content-Type: application/json" \
  -d '{"title":"Updated Title"}'
```
✅ Має повернути оновлену нотатку зі статусом 200

#### DELETE /notes/:noteId - Видалити нотатку
```bash
curl -X DELETE http://localhost:3000/notes/69307f90ef05f20679e64232
```
✅ Має повернути видалену нотатку зі статусом 200

#### Перевірка 404 помилки
```bash
curl http://localhost:3000/nonexistent
```
✅ Має повернути `{"message":"Route not found"}` зі статусом 404

---

### 2️⃣ Створити гілку та закомітити зміни

```bash
# Перевірити поточну гілку
git branch

# Якщо не на гілці 02-mongodb, створити її
git checkout -b 02-mongodb

# Перевірити зміни
git status

# Додати всі файли (крім .env - він вже в .gitignore)
git add .

# Закомітити
git commit -m "Add MongoDB integration with CRUD operations"

# Запушити на GitHub
git push origin 02-mongodb
```

⚠️ **Важливо**: Переконайтеся, що файл `.env` НЕ додано в Git (він має бути в `.gitignore`)

---

### 3️⃣ Задеплоїти на Render.com

#### Крок 1: Підготувати проєкт
- Переконайтеся, що всі зміни закомічені та запушені на GitHub
- Переконайтеся, що гілка `02-mongodb` існує на GitHub

#### Крок 2: Створити Web Service на Render
1. Зайти на https://render.com
2. Зареєструватися/увійти (можна через GitHub)
3. Натиснути **"New +"** → **"Web Service"**
4. Підключити репозиторій `nodejs-hw`
5. Вибрати гілку `02-mongodb`

#### Крок 3: Налаштування
- **Name**: `nodejs-hw-mongodb` (або будь-яка назва)
- **Environment**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Plan**: `Free`

#### Крок 4: Додати Environment Variables
У розділі **"Environment"** додати:

| Key | Value |
|-----|-------|
| `PORT` | `10000` (або залишити порожнім - Render встановить автоматично) |
| `MONGO_URL` | Ваш connection string з `.env` файлу |

⚠️ **КРИТИЧНО ВАЖЛИВО**: 
- Скопіювати `MONGO_URL` з вашого локального `.env` файлу
- Переконатися, що в connection string правильні username та password
- Переконатися, що в MongoDB Atlas налаштований Network Access (0.0.0.0/0)

#### Крок 5: Задеплоїти
1. Натиснути **"Create Web Service"**
2. Дочекатися завершення деплою (3-5 хвилин)
3. Перевірити логи:
   - Має з'явитися: `✅ MongoDB connection established successfully`
   - Має з'явитися: `Server running on port 10000` (або інший порт)

---

### 4️⃣ Перевірити задеплоєний додаток

Після успішного деплою отримаєте URL типу:
```
https://nodejs-hw-mongodb.onrender.com
```

Перевірте всі роути:
- ✅ `GET https://your-app.onrender.com/notes`
- ✅ `GET https://your-app.onrender.com/notes/:noteId`
- ✅ `POST https://your-app.onrender.com/notes`
- ✅ `PATCH https://your-app.onrender.com/notes/:noteId`
- ✅ `DELETE https://your-app.onrender.com/notes/:noteId`
- ✅ `GET https://your-app.onrender.com/nonexistent` (має повернути 404)

---

### 5️⃣ Фінальна перевірка перед здачею

- ✅ Код на гілці `02-mongodb` в GitHub
- ✅ Посилання на GitHub репозиторій готове
- ✅ Посилання на Render.com готове
- ✅ Всі роути працюють на Render
- ✅ Підключення до MongoDB працює
- ✅ Дочекатися **5 хвилин** після останнього коміту перед здачею

---

## 🆘 Якщо щось не працює

### Проблема: Render не може підключитися до MongoDB
**Рішення**: 
- Перевірити Network Access в MongoDB Atlas (має бути 0.0.0.0/0)
- Перевірити, чи правильно скопійований `MONGO_URL` в Render
- Перевірити username та password

### Проблема: Сервер на Render падає
**Рішення**: 
- Перевірити логи в Render Dashboard
- Переконатися, що `MONGO_URL` додано в Environment Variables
- Переконатися, що `PORT` встановлено (Render може встановити автоматично)

### Проблема: 404 помилки на всіх роутах
**Рішення**: 
- Перевірити, що роути зареєстровані правильно
- Перевірити, що `app.use('/notes', notesRoutes)` є в server.js

---

## 🎉 Готово!

Після виконання всіх кроків ваш проєкт готовий до здачі ментору!

