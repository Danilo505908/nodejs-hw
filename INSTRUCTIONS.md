# 📋 Інструкції: Що потрібно зробити самостійно

## ✅ Що вже зроблено:
- ✅ Встановлені пакети (mongoose, http-errors)
- ✅ Створена вся структура папок та файлів
- ✅ Написаний весь код для CRUD операцій
- ✅ Налаштовані middleware та обробка помилок
- ✅ Створена модель Note
- ✅ Створені контролери та роути
- ✅ Оновлений server.js

---

## 🎯 Що потрібно зробити ТИ:

### 1️⃣ Створити гілку та закомітити зміни

```bash
# Перевірити поточну гілку
git status

# Створити нову гілку 02-mongodb
git checkout -b 02-mongodb

# Додати всі зміни
git add .

# Закомітити
git commit -m "Add MongoDB integration with CRUD operations"

# Запушити на GitHub
git push origin 02-mongodb
```

---

### 2️⃣ Налаштувати MongoDB Atlas

#### Крок 1: Створити кластер
1. Зайти на https://www.mongodb.com/cloud/atlas
2. Зареєструватися/увійти
3. Створити новий кластер (безкоштовний M0)
4. Дочекатися створення кластеру (3-5 хвилин)

#### Крок 2: Налаштувати доступ до мережі
1. У лівому меню вибрати **"Network Access"**
2. Натиснути **"+ ADD IP ADDRESS"**
3. Вибрати **"ALLOW ACCESS FROM ANYWHERE"** (або ввести `0.0.0.0/0`)
4. Натиснути **"Confirm"**

#### Крок 3: Створити користувача бази даних
1. У лівому меню вибрати **"Database Access"**
2. Натиснути **"+ ADD NEW DATABASE USER"**
3. Вибрати **"Password"** метод автентифікації
4. Ввести **Username** та **Password** (запам'ятайте їх!)
5. Надати права **"Atlas admin"** або **"Read and write to any database"**
6. Натиснути **"Add User"**

#### Крок 4: Отримати connection string
1. У лівому меню вибрати **"Database"**
2. Натиснути **"Connect"** біля вашого кластеру
3. Вибрати **"Connect your application"**
4. Скопіювати connection string (виглядає так):
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```

#### Крок 5: Оновити connection string
Замінити в скопійованому рядку:
- `<username>` → ваш username
- `<password>` → ваш password
- Після останнього `/` додати назву бази: `notes-db?retryWrites=true&w=majority`

**Приклад:**
```
mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/notes-db?retryWrites=true&w=majority
```

---

### 3️⃣ Створити файл .env

Створити файл `.env` в корені проєкту (поруч з `package.json`):

```env
PORT=3000
MONGO_URL=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/notes-db?retryWrites=true&w=majority
```

⚠️ **ВАЖЛИВО**: 
- Замінити `your-username`, `your-password` та `cluster0.xxxxx` на свої дані
- Назви змінних мають бути точно: `PORT` та `MONGO_URL`

---

### 4️⃣ Імпортувати тестові дані в MongoDB

#### Варіант 1: Через MongoDB Compass
1. Завантажити MongoDB Compass: https://www.mongodb.com/try/download/compass
2. Встановити та відкрити
3. Підключитися, використовуючи connection string з `.env`
4. Створити базу даних `notes-db` (якщо не існує)
5. Створити колекцію `notes`
6. Натиснути **"Add Data"** → **"Import File"**
7. Вибрати файл `notes.json` з проєкту
8. Імпортувати дані

#### Варіант 2: Через MongoDB Atlas UI
1. У MongoDB Atlas перейти в **"Database"** → **"Browse Collections"**
2. Створити базу `notes-db` та колекцію `notes`
3. Натиснути **"Insert Document"**
4. Вручну додати документи з `notes.json` (скопіювати JSON)

#### Варіант 3: Через API (після запуску сервера)
Після запуску сервера можна створити нотатки через POST запити.

---

### 5️⃣ Протестувати локально

```bash
# Запустити сервер
npm run dev
```

Перевірити в браузері або через Postman:
- ✅ `GET http://localhost:3000/notes` - має повернути масив нотаток
- ✅ `GET http://localhost:3000/notes/:noteId` - має повернути одну нотатку
- ✅ `POST http://localhost:3000/notes` - створити нову нотатку
- ✅ `PATCH http://localhost:3000/notes/:noteId` - оновити нотатку
- ✅ `DELETE http://localhost:3000/notes/:noteId` - видалити нотатку
- ✅ `GET http://localhost:3000/nonexistent` - має повернути 404

**Приклад POST запиту:**
```json
{
  "title": "Test Note",
  "content": "This is a test",
  "tag": "Work"
}
```

---

### 6️⃣ Задеплоїти на Render.com

#### Крок 1: Підготувати проєкт
```bash
# Переконатися, що всі зміни закомічені
git status

# Якщо є незбережені зміни
git add .
git commit -m "Update before deployment"
git push origin 02-mongodb
```

#### Крок 2: Створити Web Service на Render
1. Зайти на https://render.com
2. Зареєструватися/увійти (можна через GitHub)
3. Натиснути **"New +"** → **"Web Service"**
4. Підключити репозиторій GitHub
5. Вибрати репозиторій `nodejs-hw`
6. Вибрати гілку `02-mongodb`

#### Крок 3: Налаштування сервісу
- **Name**: `nodejs-hw-mongodb` (або будь-яка назва)
- **Environment**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Plan**: `Free` (безкоштовний)

#### Крок 4: Додати Environment Variables
У розділі **"Environment"** додати:
- **Key**: `PORT` → **Value**: `10000` (Render автоматично встановить, але можна вказати)
- **Key**: `MONGO_URL` → **Value**: ваш connection string з `.env`

⚠️ **ВАЖЛИВО**: 
- Не додавати `.env` файл в Git (він вже в `.gitignore`)
- Додати змінні оточення безпосередньо в Render

#### Крок 5: Задеплоїти
1. Натиснути **"Create Web Service"**
2. Дочекатися завершення деплою (3-5 хвилин)
3. Перевірити логи на наявність помилок

---

### 7️⃣ Перевірити задеплоєний додаток

Після успішного деплою перевірити всі роути на вашому Render URL:
- `https://your-app-name.onrender.com/notes`
- `https://your-app-name.onrender.com/notes/:noteId`
- і т.д.

---

### 8️⃣ Фінальна перевірка перед здачею

- ✅ Код на гілці `02-mongodb`
- ✅ Посилання на GitHub репозиторій
- ✅ Посилання на задеплоєний проєкт на Render.com
- ✅ Всі роути працюють
- ✅ Підключення до MongoDB працює
- ✅ Дочекатися 5 хвилин після останнього коміту перед здачею

---

## 🆘 Можливі проблеми та рішення

### Проблема: "MongoServerError: bad auth"
**Рішення**: Перевірити username та password в connection string

### Проблема: "MongoNetworkError: connection timeout"
**Рішення**: Перевірити Network Access в MongoDB Atlas (має бути 0.0.0.0/0)

### Проблема: Сервер не запускається
**Рішення**: Перевірити, чи існує файл `.env` та чи правильно заповнені змінні

### Проблема: Render не може підключитися до MongoDB
**Рішення**: Перевірити, чи додано `MONGO_URL` в Environment Variables на Render

---

## 🎉 Готово!

Після виконання всіх кроків ваш проєкт готовий до здачі!

